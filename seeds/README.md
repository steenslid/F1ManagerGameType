# Seeds

Reference data shipped as JSON, loaded into each new save's schema at creation.

Per `information.md`, this directory should eventually hold:

- `teams.json` — 11 F1 teams + base data
- `drivers_f1.json`, `drivers_f2.json`, `drivers_f3.json`
- `personnel.json`
- `engine_suppliers.json` + `pu_versions_mk1.json`
- `tracks.json`
- `calendar_2026.json`
- `sponsors.json` + `initial_team_sponsorships.json`
- `regulation_eras.json` (with the 2026 era)
- `tyre_compounds.json`
- `part_versions_mk1.json` (per team per part type)

Nothing here yet — placeholder for the seed loader to wire against.
